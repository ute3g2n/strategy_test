# P5R2-21 物理削除受入ログ

- Step: `P5R2-21`
- Run: `RUN-P5R2-21-DELETE-LOCAL-001`
- 実行日: `2026-08-23`
- 判定: `LOCAL_GREEN`

## 開始条件

P5R2-DELETE-G1の承認状態を確認し、実装・物理削除受入の対象を新規一時Fixtureだけに限定した。既存のData、Run、Export済みCSV、Audit、Evidence、外部Provider、Secret、費用、P6は作業対象に含めなかった。

## 実施内容

1. REDテストを追加し、物理削除の未実装状態を確認した。
2. `LocalResultArtifacts`へ固定root、logical ID、server-owned状態、path safety、lock、snapshot／TOCTOU、Audit／tombstone、冪等再送、物理削除を実装した。
3. `BacktestProductService`へRun状態の権威付け、削除状態の永続化、再起動復元を実装した。未知Runは物理I/Oへ到達しない。
4. `HistoryCatalog`へ結果削除済みRunの復元分岐を追加し、結果ファイルがないことだけで復旧要求にしないようにした。
5. 実Application APIのHTTP境界とP5R2 UIへ、確認Dialog、確定後API呼出し、削除済み表示、二重操作抑止を接続した。
6. 新規一時Fixtureだけを使うPythonテスト、UI Vitest、実Application APIのPlaywright desktop／mobile、axeを実行した。

## Runtime truthfulness

指定Coordinatorは`AutoTradeProject_ImplementationQuality_Orchestrator_v0_1`、指定AgentはA110/A120/A130/A140/A150/A160/A95だった。nested named dispatchはこの環境で利用できなかったため、未起動Agentを独立完了扱いにせず、runtime receiptへ`RUNTIME_DISPATCH_FALLBACK_REQUIRED`、`agent_id=N/A`、`independent=false`、`review_mode=SELF_REVIEW_FALLBACK`を記録した。

## 検証結果

- 物理削除専用Python: `5 passed`
- P5R2対象Python回帰: `40 passed`
- UI Vitest: `14 passed`
- UI build: PASS
- UI lint: PASS（既存Fast Refresh warningのみ）
- ruff／mypy／compileall: PASS
- Playwright: desktop／mobile `2 passed`
- axe critical／serious: `0`
- 外部request: `0`

## 次工程

P5R2-21は完了。P5R2-22で、今回実装・検証した現行機能だけを`01_バックテスト手順書`へ反映し、実画像と操作説明を追加する。H2承認前のP5R2完了宣言、P6開始、外部Data取得、既存Artifactの破壊的削除は行わない。
