# P5R2-DATA-G1 承認判断

- 判断日: `2026-08-23`
- 判断: `APPROVED_BOUNDED_P5R2_18`
- 判断者: root Codex
- 権限根拠: `P5R2-HumanGate権限移譲_2026-08-22.md` に記録された、P5R2-25完了までの利用者によるHuman Gate判断権限の移譲
- packet: `doc/phase5R2/07_DATA-G1/07_P5R2-DATA-G1承認packet.html`
- 判断Evidence: `tests/evidence/phase5R2/RUN-P5R2-DATA-G1-P5R2-001/human-gate-p5r2-data-g1.md`

## 判断理由

P5R2-17 packetに、Provider、host、market、symbol、source interval、UTC期間、取得物、派生時間足、外部Run ID、保存先、通信境界、Secret境界、費用上限、停止条件、rollback条件、Unknown、P5R2-18への遷移条件が揃っている。P5R2-16のlocal統合GREEN Evidenceも確認済みであり、今回の承認を限定pilotへ閉じ込めることで、実Provider受入に必要な範囲と承認外の操作を分離できる。

## 承認した範囲

P5R2-18の `RUN-P5R2-18-EXTERNAL-001` に限り、Binance Data Vision public archiveからSpotの `BTCUSDT`／`ETHUSDT` の `1m` sourceを、UTC `2025-02-24T00:00:00Z` 以上 `2025-03-01T00:00:00Z` 未満で取得する。対象はmatching monthly kline archiveと sibling `.CHECKSUM` の最大4オブジェクトとし、`15m`／`30m`／`1h`／`4h`／`1d`はlocalで生成する。取得先は `data.binance.vision:443` のHTTPSだけ、redirect reject、proxy disabled、API key／Secret／login／REST／WebSocket／Authorizationなし、provider fee capは `0 USD` とする。local promotionは `E:\\strategy_test_data\\autotrade\\historical\\spot\\klines\\1m` へstaging／atomic promotionで行う。

## 未解決事項と停止

次のUnknownは承認によって解消しない。

1. Historical Dataの保持・再配布条件が本システムの運用に適用できるか。
2. 対象symbolのarchive完全性と更新挙動。
3. 外部Runでhost-level isolationを独立に確認できるか。

いずれかのscope、path、schema、timestamp、UTC、duplicate、gap、checksum、Secret、cost、通信境界が承認範囲と一致しない場合は開始または昇格を停止する。process-level allowlistをhost-level isolationと読み替えない。host-level isolationが確認できない場合は `NOT_VERIFIED` として停止する。

## 次の遷移

次はP5R2-18の実Provider受入準備・実行である。ただし、実行前にこの判断、request、allowlist、Run ID、Evidence root、保存先、停止条件を再照合する。P5R2-18以外の外部I/O、Data再配布、Secret、費用、物理削除は対象外である。`P5R2-DELETE-G1`、`P5R2-H2`は未承認のまま保持し、P6は開始しない。
