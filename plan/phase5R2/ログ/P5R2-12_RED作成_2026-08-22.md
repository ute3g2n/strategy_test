# P5R2-12 RED作成ログ

## 結論

判定は `QUALITY_GATE_BLOCKED`。開始条件のread-only照合で、host outbound isolation、scope実行許可、Unknown解消が成立しなかったため、REDテストの作成・実行を開始せず、GREEN実装も行わなかった。

## 確認した入力

| 対象 | 確認結果 |
|---|---|
| `doc/phase5R2/05_H1/06_P5R2-H1承認packet.html` | `APPROVED_BY_DELEGATED_AUTHORITY`。承認範囲はlocalのみ |
| `scripts/quality_gate/trusted_scopes.json` の `RUN-P5R2-11-LOCAL-001` | `phase5R2` / `target_only` / 登録のみ / `execution_allowed=false` |
| `plan/phase5R2/quality/P5R2-11_run-manifest.json` | P5R2-11の登録契約。外部I/O、Secret、物理削除は禁止 |
| 既存fixture | path/name/version/protected identityは既存trusted recordと一致。read-only参照のみ |
| Evidence root | 開始前は未作成。契約どおりの場所をBLOCKED証跡保存用に作成 |
| fixed Quality entry | 既存 `scripts/quality_gate/local_p5r_pytest.py` が存在し、scopeの固定test commandとrunner allowlistに一致 |
| P5R2 namespace | wrapperとshell runnerのphase判定は`phase5R2`を受理。固定manifest／runnerが既存local_p5r入口へdispatchする契約 |
| host outbound isolation | `NOT_VERIFIED`。確認なしのtest subprocessは契約違反となるためBLOCKED |

## RED対象と未実施状態

各atomic Requirementについて、REDにすべき失敗条件はEvidenceへ列挙したが、テストファイルは作成していない。未実装をPassへ変換しないため、8件すべてを `NOT_CREATED_BLOCKED` とした。

1. `P5R2-CREQ-TF-001`: 5つの戦略足、1m非選択、legacy M30/1m拒否。
2. `P5R2-CREQ-TF-002`: UTC boundary、effective end、closed bar、全期間未確定拒否。
3. `P5R2-CREQ-TF-003`: 単一内部欠損以外、未来値、逆転、provenance欠落を拒否。
4. `P5R2-CREQ-HD-001`: DownloadJob／GenerationJob分離、staging、validation、atomic promotion、restart/partial failure。
5. `P5R2-CREQ-HD-002`: identity、dedupe、conflict、merge preview、明示replace、影響Run/result。
6. `P5R2-CREQ-RUN-001`: 共通state、cancel、terminal不変、二重押下、再送、別tab、OperationGuard。
7. `P5R2-CREQ-RUN-002`: CSV保護、active Run拒否、path traversal/symlink/reparse/別ID拒否。
8. `P5R2-CREQ-DOC-001`: 操作者、理由、target、旧/新state、依存数、request/correlation ID、失敗理由、Manual registry、migration failure。

## runtimeとA95

`multi_agent_v1__spawn_agent` / `multi_agent_v1__wait_agent` によりroot planner probe（agent_id=`01a02990-9221-7cc3-8aad-f06ee9e1e91d`、nickname=`Bacon`、model=`gpt-5.6-luna`、reasoning=`max`、service tier=`priority`）を実行し、wait完了した。要求したCoordinatorとA110/A130/A95へのnested dispatchは確立しなかったため、指定部品は `NOT_BOUND` / `NOT_DISPATCHED`、`agent_id=N/A`、`independent=false`、`review_mode=SELF_REVIEW_FALLBACK` と記録し、独立実行済みとは扱っていない。定義JSONのmodelはruntime receiptへ記録した。

A95の独立起動はできなかったため、root fallbackの静的判定として、管理用hash、manifest fingerprint、stale、retry、receipt hashを追加しないことを確認した。既存protected fixture identityは既存recordをread-only参照し、値の再計算・新規identity作成はしていない。

## 禁止操作と回復

外部network、Secret、Provider login/API/download、費用、既存Historical Data/Run/Audit/Evidence/Export済みCSVの削除、Playwright/npm、production source変更、GREEN実装は行っていない。temporary fixtureも作成していない。

失敗分類は環境（host isolation未確認、scope registration-only、Unknown未解消）。debug recoveryの同一仮説に対する再試行は0回で、失敗を握りつぶしていない。

再開条件は、固定scopeの実行可否をP5R2-12用に明示し、host outbound isolation、既存protected fixture接続、Evidence root契約を確認し、P5R2-UNK-QG-001/002をEvidenceで更新することである。これらが揃うまでP5R2-12のtest subprocessは開始しない。

機械証跡: [P5R2-12_RED.json](../../tests/evidence/phase5R2/RUN-P5R2-11-LOCAL-001/P5R2-12_RED.json) / [runtime receipt](../quality/runtime-receipt-P5R2-12.json)

## 追補：preflight後のRED実行（2026-08-22）

上記の`QUALITY_GATE_BLOCKED`記録は、preflight前の履歴として保持する。その後、P5R2-12 preflightで次を確認した。

| 確認対象 | 結果 |
|---|---|
| host outbound isolation | `CONFIRMED`。WSL2 `networkingMode=none`、default routeなし、外向きNICなし |
| protected fixture | 既存identityとの一致をread-onlyで確認。新規checksum／identityは作成していない |
| fixed Quality entry | `scripts/quality_gate/local_p5r2_pytest.py` を `phase5R2` の固定test entryとして実行 |
| 前段Gate | formatter／lint／type はすべて `PASS` |
| RED test Gate | `FAIL_EXPECTED_RED`。P5R2契約未実装APIを検出したため、GREEN実装は行っていない |
| 補助診断 | Windows側read-only診断で `306 passed / 29 failed`。skip／xfailは0件 |

8件のatomic Requirementについて、対応するRED契約テストファイルと失敗条件をEvidenceへ記録した。対象は時間足3件、Historical Data 2件、Run操作／Result Artifact 2件、Manual／Evidence 1件である。

固定4 Gate実行中に、既存P5R対象コード・テストのformatter／lint／型注釈のみを補正した。これはP5R2契約のGREEN実装ではない。外部network、Provider login/API/download、Secret、費用、既存Data／Run／Audit／Evidence／Export CSVの削除、Playwright/npmは行っていない。

P5R2-12の判定は`RED_CONFIRMED`。次はP5R2-13で、時間足／PreflightのREDをGREENへ進める。機械証跡：[RED結果](../../tests/evidence/phase5R2/RUN-P5R2-11-LOCAL-001/P5R2-12_RED.json) / [固定4 Gate](../../tests/evidence/phase5R2/RUN-P5R2-11-LOCAL-001/verification.json) / [runtime receipt](../quality/runtime-receipt-P5R2-12.json)
