# Phase 2 パイロット評価（S4.1 読取り評価）

> **この評価後の現在状態は [全Phase 残課題・Blocked 統合台帳](../../doc/00_全Phase残課題Blocked統合台帳.html) で管理します。** 本書はS4.1時点の評価証拠であり、ここに書かれたツール不足などの一部は後続S4.2〜S4.4で解消済みです。

作成日: 2026-08-06  
対象 Run: `RUN-P2-IC-001`  
Orchestrator: `AutoTradeProject_ImplementationQuality_Orchestrator_v0_1`  
判定: **BLOCKED（Pass不可）**

## 1. 評価範囲と固定値

P2-D07 `CatalogResolver.resolve`、REQ-Q02 / REQ-Q19 / REQ-Q20 / REQ-Q23、HEAD `8d3f3d3dd41b6d5b33e6b870a3f5b4f1b10ffab4`、指定された実装・テスト・fixture・証跡・品質ゲート・Orchestrator定義を読み取った。固定 fixture の SHA-256、対象パス、除外パス、差分ハッシュは [baseline.json](../../tests/evidence/phase2/RUN-P2-IC-001/baseline.json) に記録した。

現ワークツリーには、pilot対象外の計画書変更がある。baselineの `worktree_sha256_before_this_evidence_record` はこの評価証跡を追加する前の観測値であり、証跡追加後の再計算を要求する。

## 2. 分類別評価

| 分類 | 判定 | 重要度 | 原因 | 証拠パス | 最小改訂案 | 影響パス |
|---|---|---|---|---|---|---|
| 仕様 | 要改訂 | Medium | P2-D07 と4要件の参照はあるが、設計で未確定の契約・ロール関連を `unknowns: []` としており、未確定事項を明示した証跡になっていない。 | `doc/phase2/03_市場データ詳細設計/07_Instrument_Catalog詳細設計書.html`、`tests/evidence/phase2/RUN-P2-IC-001/run-manifest.json` | fixture-only の対象外事項を `N/A` と明記し、設計由来の Unknown はIDと停止条件を Manifest または評価記録へ転記する。 | `run-manifest.json`、`04_パイロット評価.md` |
| 実装 | 条件付きPass | Low | 実装は in-memory fixture を受け取り、UTC・期間・active・tick size・監査失敗を fail-closed にしている。0件と複数件の理由が同一で診断性は低い。 | `src/autotrade/market_data/catalog_resolver.py`、`reviews/python-review.md`、`reviews/trading-safety-review.md` | 既存APIを変えず、0件と複数件の reason を分離する追加テストを次回改訂で行う。 | `src/autotrade/market_data/catalog_resolver.py`、`tests/market_data/test_catalog_resolver.py` |
| テスト | 要改訂 | Medium | RED は未実装機能のアサーション失敗ではなく `ModuleNotFoundError` で、レビュー記録のテスト数（8件）と現行テスト数（9件）も一致しない。 | `tests/evidence/phase2/RUN-P2-IC-001/tdd-evidence.md`、`tests/market_data/test_catalog_resolver.py`、`verification.json` | import path を固定した真のREDを再記録し、9件のテスト一覧・期待値・実行結果を一致させる。 | `tests/market_data/`、`tests/evidence/phase2/RUN-P2-IC-001/` |
| ツール設定 | BLOCKED | High | Runner の `TRUSTED_TARGET_PATHS` が `scripts/quality_gate` と `tests/quality_gate` に固定され、P2-D07を拒否する。ruff/mypy/pyrightが未導入で、Manifestの `change_hash` も未解決である。 | `scripts/quality_gate/runner.py`、`tests/evidence/phase2/RUN-P2-IC-001/implementation-quality-preflight.md`、`run-manifest.json` | Component Lifecycleで承認済みRun IDごとの固定scope registryをTDD実装し、ツールを固定バージョンで導入またはBLOCKEDにする。実差分からhashを計算して照合する。 | `scripts/quality_gate/`、`.codex/orchestrators/AutoTradeProject_ImplementationQuality_Orchestrator_v0_1.json` |
| エージェント指示 | 要改訂 | High | OrchestratorはHuman Gate承認記録を要求するが、現記録は `HUMAN_GATE_REQUIRED` で未承認。レビューも実Agent実行ログではなく「相当」と記載され、独立性と実行証跡が不足する。 | `tests/evidence/phase2/RUN-P2-IC-001/human-gate.md`、`reviews/python-review.md`、`reviews/trading-safety-review.md`、Orchestrator JSON | A150/A160/A130の実行識別子・コマンド・入力hash・Finding IDを記録し、Human Gateは署名検証可能な権限者承認が揃うまで停止する。 | `.codex/agents/`、`tests/evidence/phase2/RUN-P2-IC-001/` |
| 外部依存 | Pass（検査範囲内） | なし | 対象実装は外部I/O、ネットワーク、Databento、Broker、Secret、実データを参照しない。Orchestratorも禁止条件を明示している。 | `src/autotrade/market_data/catalog_resolver.py`、`scripts/quality_gate/local_pytest.py`、Orchestrator JSON | 現状維持。Runnerの外部接続禁止をP2 scope registryの停止条件にも重ねる。 | `src/`、`scripts/quality_gate/` |

## 3. 明示確認項目

| 確認項目 | 結果 | 根拠 |
|---|---|---|
| Runner S2 bootstrap scope 固定 | BLOCKED要因 | `TRUSTED_TARGET_PATHS = ("scripts/quality_gate", "tests/quality_gate")`。ManifestのP2 target_pathsと不一致。 |
| ruff | 未導入 | `.venv/Scripts/python.exe -m ruff --version` が `No module named ruff`。 |
| mypy | 未導入 | `.venv/Scripts/python.exe -m mypy --version` が `No module named mypy`。 |
| pyright | 未導入 | `.venv/Scripts/python.exe -m pyright --version` が `No module named pyright`。 |
| change_hash | 未解決 | Manifest値が `UNRESOLVED-QUALITY-GATE-SCOPE`。 |
| Human Gate | 未承認 | `human-gate.md` が `HUMAN_GATE_REQUIRED`。 |
| 外部接続 | 使用なし | 対象コードとローカル検査に禁止対象への接続参照なし。 |

## 4. 次の停止解除条件

次の順序で再実行する。いずれかが失敗したら後続へ進めず `BLOCKED` とする。

1. P2-D07 scope、Unknown、対象外変更を baselineと照合する。
2. TDDでRunnerのP2固定scope、Manifest改ざん検出、skip/deletion検出を追加する。
3. formatter / lint / type / pytest の固定版を用意し、全コマンドの終了コード・version・hashを記録する。
4. A130、A150、A160の独立再レビューを実ログとFinding ID付きで保存する。
5. 権限者の署名済みHuman Gate承認を検証し、全Critical/High・証跡欠落・設計外変更が0の場合だけPassにする。

現時点では、ツール設定とHuman GateのHigh指摘が残るため、Passにはしない。

## S4.2 実装後の更新

`AutoTradeComponentLifecycle_Orchestrator_v0_1` と `AutoTradeProject_ImplementationQuality_Orchestrator_v0_1` の更新契約に従い、次を追加・改訂した。

- `scripts/quality_gate/trusted_scopes.json`: `RUN-P2-IC-001` の固定scope、fixture checksum、4 Gateを登録。
- `scripts/quality_gate/local_p2_pytest.py`: `tests/market_data`だけを実行し、socket接続とpytest plugin自動読込を拒否。
- `scripts/quality_gate/runner.py`: registry照合、Manifest改変拒否、差分停止、host isolation未確認時のBLOCKEDを実装。
- TDD証跡: RED（wrapper未実装による収集失敗）後、品質ゲート33件・全体42件がGREEN。
- 独立レビュー: Pythonコード上のCritical/Highは0件。取引安全レビューはHuman Gate未承認をHighとして残し、実RunはBLOCKED。

ruff / mypy / pyright未導入、`change_hash`未解決、host isolation未確認、Human Gate未承認が残るため、実Runの4 Gate実行とPass判定は行っていない。
## S4.3 Python 品質ツール設定・証跡

- `requirements-dev.txt` と `pyproject.toml` に `ruff==0.16.1`、`mypy==2.3.0`、`pytest==9.1.1`、`pytest-cov==7.1.0` を固定記載した。取得はユーザー許可済みの PyPI から `.venv` へ行い、Runner 自身のネットワーク接続は発生させていない。
- `src/autotrade/market_data` と `tests/market_data` に対する formatter、lint、type、固定 P2 pytest wrapper、coverage は終了コード 0。coverage は 88.43%（閾値 80%）。
- fixture SHA-256 は `sha256:94022229698e972353b8ec9537f455af5cb29d47253f5f2a1ed5d33b08b50169`。Runner 再計算値と Manifest の `change_hash` は `verification.json` と `run-manifest.json` に記録した値で一致した。
- root の `.coverage` だけを確認して `git rm --cached -- .coverage` を実行し、`.gitignore` に追加した。物理ファイルは保持し、履歴は書き換えていない。
- 最終状態は `BLOCKED`。Runner が `target_paths` 外の変更を検出し、Human Gate も未承認のため、4 Gate 成功だけでは Pass に進めない。詳細は [S4.3 証跡](../../tests/evidence/phase2/RUN-P2-IC-001/s4-3-tooling.md) と [verification.json](../../tests/evidence/phase2/RUN-P2-IC-001/verification.json) を参照。
