# P3-08R-02 実行ログ
実行日: 2026-08-10（Asia/Tokyo）
Run ID: `RUN-P3-POC-READY-001`
状態: `PASS`（P3-08R-02の入口・Adapter契約のみ）
準備状態: `IN_PROGRESS`（P3-09は未実行）
実装commit: `b9f3c77f4f449808ec27d2c167cbea9435618068`

## 1. 実施範囲

P3-08R-01の固定input contractとP3-08Aの固定digestを受け取り、P3-09専用の準備入口、LEAN project/config契約、machine-readable output schema、fail-closed検証を追加した。

実施していない範囲:

- LEAN/NautilusTrader/Broker/Paper/Live/Cloud/Secretの起動・接続
- 実データ取得、外部通信、P3-09実測結果の生成
- Core reference、expected parity output、P3-09本Manifestの確定
- `RUN-P3-POC-001`の`execution_allowed`変更

## 2. 作成物

- `tests/engine_poc/entrypoint.py`
- `tests/engine_poc/test_entrypoint.py`
- `tests/engine_poc/lean_project/config.schema.json`
- `scripts/quality_gate/local_p3_poc.py`
- `tests/evidence/phase3/RUN-P3-POC-READY-001/local-prepare-contract-output.json`
- `tests/evidence/phase3/RUN-P3-POC-READY-001/p3-08r-02-verification.json`
- A40/A150/A160レビュー

## 3. 入口の契約

`local_p3_poc.py --mode prepare`は、固定contractを読み、fixture hash、P3-08A digest、P3-09 fire-control、固定path、Manifestが存在する場合のbindingを検証する。R-02時点ではManifestが未作成なので、結果は`CONTRACT_READY`である。

返却結果には次を含む。

- `engine_started=false`
- `p3_09_start_allowed=false`
- `network.required_mode=none`、`network.observed=false`
- input root、read-only、write root、Local、Cloud/Broker/Secret禁止
- 固定LEAN image index/Linux amd64 digestとentrypoint

`--mode run`は`P3_09_EXECUTION_NOT_IMPLEMENTED_IN_PREPARE_ENTRY`でexit code 2となり、実engineを起動しない。

## 4. RED/GREEN

初回は専用entrypoint未作成によるimport failureとなった。その後、CLI直接起動時のsrc-layout import failureも検出した。正本`src`をCLIのsys.pathへ固定し、契約テストを再実行した。

| Gate | 結果 |
|---|---|
| formatter | PASS |
| lint | PASS |
| type | PASS（mypy 50 source files） |
| 契約test | PASS（20 passed） |
| `prepare` | `CONTRACT_READY`、engine未起動 |
| `run` fire-control | `STOPPED`、exit code 2 |

## 5. レビュー判定

A40、A150、A160のCritical/High/Mediumは0件。入口はP3-09実行を許可するものではなく、P3-08R-03でCore reference・expected output・Manifestを確定する前提で次へ引き渡す。

## 6. 次Step

P3-08R-03でCoreをreference側として一回だけ実行し、`expected/core-reference.json`、`expected/lean-output-schema.json`、`expected/parity-map.json`、`run-manifest.json`を固定する。
