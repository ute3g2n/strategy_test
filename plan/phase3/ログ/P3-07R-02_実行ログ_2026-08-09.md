# P3-07R-02 実行ログ

- Run ID: `RUN-P3-BT-REPAIR-002`
- 実行日: 2026-08-09
- 対象: 型付きReplay、P2 binding、Calendar/時間足接続、Strategy一回呼出し、保守的仮想約定

## 実装

- `src/autotrade/backtest/contracts.py` に不変DTO、EngineAdapter Protocol、厳格canonical JSONを追加。
- `src/autotrade/backtest/runner.py` に型付きBacktest Core実行経路を追加。
- Replay順序、重複、品質、Manifest、Calendar、直接M30、同時close cohort、Directive/Fillを接続。
- `src/autotrade/strategy/service.py` は派生時間足と同一物理M1 source IDを同時に扱えるよう、view単位の重複検証へ最小修正。
- legacy bool/dict入口は正本経路から外し、証拠不足時は構造化STOPPEDへ変更。

## 検証

- Ruff: PASS
- `tests/backtest/test_backtest_repair_red_contract.py` + `tests/backtest/test_backtest_repair_core.py` + `tests/strategy`: `160 passed`
- compileall: PASS
- Frozen fixture変更: なし

## 旧契約との相違

旧P3-D05 Backtestテスト15件は、入力bool/dictだけでPASSする期待値を持つため、P3-07R-01受入REDの「証拠不足はSTOPPED」と同時には成立しない。仕様・fixture・期待値を変更せず、対象Run証跡へ明記した。Phase 3の全体受入で扱う際は、旧契約を正本扱いしないことを確認する。

## 引き渡し

P3-07R-03へ、Manifest/Snapshot/append-only ResultStore/atomic publish/recoveryの実装境界を引き渡す。Cost、Slippage、Roll、Gap、Broker、Paper、Liveは本Runで実装・合格主張しない。

## Legacy contract migration completion

The 15 old fail-open assertions were migrated under the user-approved policy 1. Frozen fixture files were not changed. The migrated tests now verify structured `STOPPED` for bool/dict-only or unobserved evidence, while typed execution and permutation coverage remain in `test_backtest_repair_core.py`.

Final full-scope result: `pytest tests/backtest tests/strategy -q` = `240 passed`.
