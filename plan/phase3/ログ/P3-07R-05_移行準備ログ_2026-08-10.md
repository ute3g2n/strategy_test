# P3-07R-05 移行準備ログ（2026-08-10）

## 判定

`RUN-P3-BT-001`は、P3-07R-05の正式再実行へ移行できる状態まで整備した。最終受入はまだ確定していない。

- コード変更hash: `sha256:9ef8cdd54d6579f557085c2b240380810167d8602e3cd2aed30b8954b88c06b7`
- fixture hash: `sha256:aeb03df1eef3ea836d176a8b0443c45b6bc7f6d01e455fd6026cabf16c536fa4`
- review: Critical 0 / High 0
- R-05固定4 Gateの補助実行: formatter PASS、lint PASS、mypy PASS、固定P3 wrapper 258 passed
- 全テスト: 406 passed
- 登録Runner: `BLOCKED`（host outbound isolation未確認）

## 実施内容

1. Windowsで発生していたR-05 type Gateの22件を、入力値の型絞り込み・Windows非対応APIの動的参照・ResultRow/CommitMarkerの明示変数化で修正した。
2. `run_test.ps1`がtrusted scopeの`phase_id`からEvidenceパスを選び、P3ではphase3用隔離shellへ分岐するようにした。P2の既存DBN入口・phase2証跡は維持した。
3. P3用隔離shellは、固定fixtureの前後hash、WSL kernel、default route、外向きNIC、host-isolation evidenceを確認してから登録品質Runnerを起動する。
4. R-05 Manifestのtarget paths、change hash、review状態を現行実体へ同期した。
5. R-05 verification、acceptance traceability、A150/A160/A40レビュー、P3-D08、統合台帳を現在状態へ同期した。

## 残る再開条件

WSL clone `/home/oue/strategy_test` はdirtyかつWindows側HEADより古い。次回実行前に、WSL側の未コミット証跡を人手で整理し、対象cloneがcleanでorigin/branch/HEADが期待値であることを確認する。その後、native Windows PowerShellから次を実行する。

```powershell
.\scripts\wsl_quality_gate\run_test.ps1 -RunId RUN-P3-BT-001
```

host isolationが確認されない場合は、Runnerは引き続きBLOCKEDでよい。`QUALITY_GATE_NETWORK_ISOLATION_CONFIRMED=1`をWindows側で自己設定してはならない。

## 範囲外

LEAN/Nautilusの導入・実行、Broker、Paper、Live、Secret、実取引所Calendar、P3-09の正式30分/8GiB性能判定は開始していない。
