# RQU-UI-13 改訂統合・再試験・最終候補記録

## 1. 実行情報

| 項目 | 内容 |
|---|---|
| Step ID | `RQU-UI-13` |
| Phase ID | `REQUIREMENTS_UI_UPDATE_2026_08_11` |
| 対象 | 要件candidate、React UIモック、静的UIモック、追跡表、受入確認表 |
| 固定Seed | `20260811` |
| 発火条件 | RQU-UI-12の全Finding（Critical 0 / High 8 / Medium 4 / Low 1）を確認済み |
| 正式化 | 未実施。RQU-UI-H3承認前のcandidateのまま |
| 外部接続 | なし。Broker、市場データ、Secret、外部通知、実注文へ接続していない |

## 2. Findings first：採否と残リスク

| Finding | 重大度 | 採否 | 反映内容 | 残リスク・証拠 |
|---|---:|---|---|---|
| `RQU-UI-12-F-001` Live自動承認とHuman Gateの不接続 | High | 採用 | `gateMode`と`autoApproval`を同期し、確認・取消／スキップ（自動承認）の両方でRisk・確認Dialog・操作記録を通す | 実Live接続は未実証。`RQU-UI-13-T01`、後続Gate |
| `RQU-UI-12-F-002` 取消・停止・再開の意味差 | High | 採用 | Run取消・再実行、運用単位一時停止・再開、Forward停止を確認Dialogへ統一 | 実Worker・実注文の状態変更は未接続。`RQU-UI-13-T02` |
| `RQU-UI-12-F-003` 網羅検証入力が表示のみ | High | 採用 | 下限・上限・ステップ6項目を編集可能化、設定JSON候補選択、組合せ数再計算 | JSON内容の妥当性検査と実計算は後続実装。`RQU-UI-13-T03` |
| `RQU-UI-12-F-004` 重複判定が特定ルールに限定 | High | 採用 | `symbol × timeframe`だけで既存単位との重複を判定し、ルール差を許可しない | 編集対象単位の例外規則は詳細設計で確定。`RQU-UI-13-T04` |
| `RQU-UI-12-F-005` Strategy変更の理由・確認不足 | High | 採用 | 理由入力を必須化し、有効化・無効化・新版保存・ロールバックをDialogと記録へ接続 | 実版ハッシュ・反映順序は後続実装。`RQU-UI-13-T02` |
| `RQU-UI-12-F-006` 自PC・スマホのアクセス境界不足 | High | 部分採用 | 認証不要を閉じた自PC／本人スマートフォンのUI方針とし、candidateは外部公開しないと明記 | 実配備の端末制限、ネットワーク、TLS、公開範囲は後続Gate。UnknownをPassにしない |
| `RQU-UI-12-F-007` Drawer遷移中のVisual撮影 | High | 採用 | PC/スマホのDrawer閉鎖・CSS遷移完了待ちを共通化し、静的重要7画面を個別Visual比較 | 画像ファイルはGit管理外。`RQU-UI-13-T07` |
| `RQU-UI-12-F-008` 21画面×10状態の証拠不足 | High | 部分採用 | React版で全210セルをPC/スマホ各々操作・表示し、静的版は表示専用範囲へ限定 | Staticの状態変更は対象外。`state-matrix-react-*.json` |
| `RQU-UI-12-F-009` H1/M30フィルタ欠落 | Medium | 採用 | SCREEN-11の時間足選択肢へH1/M30を追加 | 実データの時間足生成は後続Gate |
| `RQU-UI-12-F-010` 静的HTMLの危険操作が表示のみ | Medium | 部分採用 | 静的版は表示・導線確認専用、操作可能性・Dialog・状態変更はReact版で検証すると文書化 | Staticの実処理は未実装。範囲差を誤認させない |
| `RQU-UI-12-F-011` 性能未測定 | Medium | 保留 | Q-243の性能は固定値をPassにせず後続Gateへ | 実PC負荷、20〜40単位、遅延はUnknown |
| `RQU-UI-12-F-012` 監査・Backup未確定 | Medium | 保留 | UIの表示・導線だけ候補化し、保存先・復元・世代管理は後続Gateへ | 実保存・復元証拠はUnknown |
| `RQU-UI-12-F-013` lint/Storybook警告 | Low | 部分採用 | 既存Fast Refresh警告5件を残し、機械Gateのエラー0と分離記録 | 警告は残存。機能受入を阻害しない低優先度 |

Critical/Highの未解決Findingは0件。F-006/F-008は範囲を明示した部分採用、F-011/F-012はMediumの後続Gateとして保留した。保留をPassへ変更していない。

## 3. 変更対象と同期結果

| 対象 | 変更 |
|---|---|
| 要件candidate Markdown | `candidate-0.6`。Live自動承認、危険操作、網羅検証入力、重複、端末境界、静的/React検証範囲を追記 |
| 要件candidate HTML | `candidate-0.6`。Markdownと同じ意味・使用手順・静的版の境界を同期 |
| React UI | Live Gate、確認Dialog、理由入力、設定JSON候補、編集可能な下限/上限/ステップ、重複判定、H1/M30、全画面状態操作を反映 |
| 静的UI HTML | `candidate-0.2`。表示・導線専用、外部通信なし、閉じた端末前提を明記 |
| 追跡マトリクス | `RQU-UI-13-T01`〜`T08`、210セル状態証跡、画像Git管理外を追記 |
| 受入確認表 | AC-UI-004〜031、041〜045、052を再判定。Q-243（AC-UI-035〜038）と実接続（AC-UI-053）はOpen/Blocked維持 |
| 実行ログ | UI13の採否、再試験、残Unknown、次のH3停止条件を追記 |

## 4. 再試験

### 4.1 機械Gate

| 確認 | 結果 |
|---|---|
| `npm run build` | PASS |
| `npm run lint` | PASS_WITH_WARNINGS（既存Fast Refresh警告5件、エラー0） |
| Vitest | PASS（9 tests） |
| Playwright React/Static、PC/スマホ | `RQU-UI-11` spec 18 tests、skip 0、unexpected 0（最終再実行で確定） |
| React状態マトリクス | PC/スマホ各210/210セル PASS |
| Static外部通信 | `externalRequests: []` |
| axe | React/Static×PC/スマホ、21画面、critical/serious 0 |
| Visual | React基準画像、Static全21画面、重要7画面を遷移完了後に比較 PASS |
| `git diff --check` | PASS |

### 4.2 証跡

- `tests/evidence/requirements_ui/RQU-UI-11-20260811-1325/results.json`
- `tests/evidence/requirements_ui/RQU-UI-11-20260811-1325/react-route-summary-*.json`
- `tests/evidence/requirements_ui/RQU-UI-11-20260811-1325/static-route-summary-*.json`
- `tests/evidence/requirements_ui/RQU-UI-11-20260811-1325/a11y-*.json`
- `tests/evidence/requirements_ui/RQU-UI-11-20260811-1325/state-matrix-react-*.json`
- `plan/requirements_update/RQU-UI_UIモック受入確認表_2026-08-11.md` §9
- Playwright PNG・Visual baseline・snapshotは`tests/evidence/requirements_ui/**/*.png`および`ui/mock/tests/**/*-snapshots/*.png`としてGit管理外

## 5. Q-243の今回確認と後続Gate

| 領域 | 今回閉じた確認 | 後続Gateへ残す内容 | 状態 |
|---|---|---|---|
| 安全境界 | Risk未入力停止、Kill/停止Dialog、照合前再開不可、Live自動承認のHuman Gate記録 | 実Broker・実注文・実機・Live具体値 | OPEN（後続Gate） |
| 初期候補 | MCL/M6A/MZC/MZS/MZW、4 Asset Class、5時間足の表示 | 実シンボル、取引所、限月、Roll、契約、Provider | OPEN（後続Gate） |
| 実行可能性 | React/TypeScript UI、固定Seed、静的/Reactの外部通信なし境界、Playwright証跡 | Python処理、最終Engine、実データ、負荷、復旧、Broker照合 | OPEN（後続Gate） |
| 性能 | 初回/追加/CSV/同時運用の受入項目と未測定境界を表示 | 実PC負荷、20〜40単位、遅延、復旧実測 | OPEN（Unknown） |

## 6. H3提示パッケージと停止条件

H3提示対象は、`candidate-0.6`要件Markdown/HTML、静的`candidate-0.2`、React UIソース、追跡マトリクス、受入確認表、UI11/12/13記録、最終`results.json`である。RQU-UI-H3承認前は正式要件HTML、`doc/index.html`、正式UI正本を変更しない。

H3で確認する事項は、(1)全機能と使用手順、(2)21画面・10状態・PC/スマホの意味、(3)危険操作とHuman Gate、(4)candidateと正式版の差分、(5)実接続・性能・Backup・端末境界のUnknownである。承認されない場合はRQU-UI-14へ進まず、candidateと証跡を保持する。

## 7. 変更履歴

| 版 | 日付 | 内容 |
|---|---|---|
| `v0.1` | 2026-08-11 | RQU-UI-12の13 Findingsを採否分類し、High指摘の修正範囲とRQU-UI-13再試験計画を確定した。 |
| `v0.2` | 2026-08-11 | candidate、React/静的UI、追跡表、受入表を同期し、Playwright/Visual/a11yと状態マトリクスを実行した。正式化はH3後に保留。 |
