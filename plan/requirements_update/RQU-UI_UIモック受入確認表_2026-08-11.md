# RQU-UI UIモック受入確認表（初版）

## 0. 文書情報

| 項目 | 内容 |
|---|---|
| 文書ID | `RQUUI-ART-ACCEPT` |
| 対象Step | `RQU-UI-05`（初版。実装後はRQU-UI-11で結果を追記） |
| 作成日 | 2026-08-11 |
| 対象 | 21画面、10ナビゲーション、10共通状態、67ユースケース、危険操作、Q-243割当 |
| 正式合否 | Critical/Highの1件でも失敗・未実行・不安定なら全体停止。UnknownはPassにしない |
| 証拠種別 | `TRACE`（追跡表）、`DOC`（文書確認）、`E2E`（Playwright固定旅程）、`VISUAL`（PC/スマホ画像比較）、`A11Y`（axe/keyboard/focus）、`UNIT`（component/Vitest）、`MANUAL`（運用者確認）、`GATE`（Human Gate記録） |

## 1. 判定ルール

| 判定 | 意味 |
|---|---|
| `PASS` | 固定条件の証拠があり、期待値を満たす |
| `FAIL` | 期待値を満たさない。Critical/Highは直ちに全体停止 |
| `BLOCKED` | 承認、外部接続、実データ、実機などの前提が未承認。Passへ変更しない |
| `NOT-APPLICABLE` | 本確認表で対象外。理由と代替証拠を必ず記載 |
| `NOT-RUN` | まだ実行していない。RQU-UI-11で実行するまで未完了 |

## 2. 受入確認項目

| AC-ID | 重要度 | 対象／追跡 | 確認内容 | 期待値 | 証拠種別 | 初版状態 |
|---|---|---|---|---|---|---|
| `AC-UI-001` | Critical | 全67UC | UC-ID、REQ-ID、SCREEN-ID、UISTATE-ID、E2E-IDが辿れる | 67/67件が一意に辿れる | TRACE | PASS（RQU-UI-05文書） |
| `AC-UI-002` | Critical | 21画面 | 全画面にSCREEN-ID、NAV-ID、表示、入力、Button、Dialog、遷移、状態、PC/スマホ差がある | 21/21件に欠落なし | TRACE,DOC | PASS |
| `AC-UI-003` | Critical | 10状態 | NORMAL、LOADING、EMPTY、REQUIRED、WARNING、STOPPED、FAILED、RECOVERY、HUMAN-GATE、UNAPPROVEDを定義 | 10/10件に理由・影響・不可操作・次操作がある | TRACE,DOC | PASS |
| `AC-UI-004` | High | 21×10状態 | 各画面の状態適用またはN/A理由を確認 | 210セルすべて判定、空欄0 | TRACE,DOC,E2E | PASS（RQU-UI-13 React 210/210） |
| `AC-UI-005` | Critical | 単一運用者 | Login、ユーザー管理、Role管理を表示しない | 認証不要。ただし安全確認・Human Gateは表示 | DOC,E2E | PASS（RQU-UI-13） |
| `AC-UI-006` | Critical | `ADD-UC-025/026` | 銘柄×時間足×ルールの独立単位と重複制約 | 異なる足は同時可。同じ銘柄×足の重複は開始不可 | E2E,TRACE | PASS（RQU-UI-13） |
| `AC-UI-007` | High | `ADD-UC-010/011` | Asset 4種類、初期5候補、5時間足 | MCL/M6A/MZC/MZS/MZWとD1/H4/H1/M30/M15が表示される | E2E,DOC | PASS（RQU-UI-11/13） |
| `AC-UI-008` | High | `ADD-UC-021` | Turtle System 1/2と戦略×時間足の自由な組合せ | 初期戦略が選択でき、組合せを単位へ渡せる | E2E,DOC | PASS（RQU-UI-13） |
| `AC-UI-009` | Critical | `ADD-UC-030〜041` | 単一Runと網羅検証を別Tab・別結果にする | 1セットと全組合せが混同されない | E2E,TRACE | PASS（RQU-UI-11/13） |
| `AC-UI-010` | Critical | `ADD-UC-030/039` | 網羅検証の下限・上限・ステップ・組合せ数・見積 | 全パラメータ組合せと警告を表示。未入力は開始不可 | E2E,DOC | PASS（RQU-UI-13） |
| `AC-UI-011` | High | `ADD-UC-033/034` | Queue、進捗、取消、停止、再Run | Buttonごとに結果・確認・取消・失敗理由がある | E2E | PASS（RQU-UI-11/13） |
| `AC-UI-012` | High | `ADD-UC-036/073` | 5指標（総損益、最大の落ち込み、取引回数、勝率、総残高） | 単一RunはKPI、網羅検証は全組合せ表で表示 | E2E,UNIT | PASS（RQU-UI-11/13） |
| `AC-UI-013` | High | `ADD-UC-037/038` | 絞込、並替、比較、最良自動選択なし | 条件と比較結果が保持され、判断は運用者に残る | E2E | PASS（固定ダミーの表示・導線。実計算は後続） |
| `AC-UI-014` | High | `ADD-UC-040/084` | CSV・レポート非同期出力 | 進捗、完了、失敗、取消を表示し外部送信しない | E2E,UNIT | PASS（固定ダミーの受付表示） |
| `AC-UI-015` | High | `ADD-UC-041/085/086` | 同一条件再Run、最新表示、内部保存、非表示、削除 | 同一条件を許可し、削除は確認・監査記録付き | E2E,TRACE | PASS（RQU-UI-11/13） |
| `AC-UI-016` | Critical | `ADD-UC-050〜054` | Forward→Shadow→Paper→Live候補→Liveの順序 | 前段確認なしに次モードへ進めない | E2E,DOC | PASS（RQU-UI-11/13） |
| `AC-UI-017` | Critical | `ADD-UC-053/054/083` | Human Gateと認証の区別 | 認証入力なし。対象・Risk・証拠・確認内容をDialogで表示 | E2E,MANUAL | PASS（RQU-UI-11/13） |
| `AC-UI-018` | Critical | `ADD-UC-062/063` | Risk未入力の開始禁止、値範囲検査なし | 未入力だけ止め、範囲妥当性検査を行うと表示しない | E2E,DOC | PASS（RQU-UI-11/13） |
| `AC-UI-019` | Critical | `ADD-UC-064` | 確認・取消モード／Live自動承認モード | 2モードを選べ、切替・対象・時刻を記録 | E2E,MANUAL | PASS（RQU-UI-13両モード） |
| `AC-UI-020` | Critical | `ADD-UC-074` | 全体Kill Switch | 二段確認後に全体停止。解除まで新規注文不可 | E2E,MANUAL | PASS（component/E2E固定ダミー） |
| `AC-UI-021` | Critical | `ADD-UC-027/075〜077` | 停止後の照合と手動再開 | データ・注文・Position照合完了前に再開不可 | E2E,MANUAL | PASS（状態・導線固定ダミー） |
| `AC-UI-022` | High | `ADD-UC-055/056/070` | 全運用インスタンスの状態表示 | 全単位の時刻、Signal、Position、警告、Heartbeatを確認できる | E2E | PASS（RQU-UI-11） |
| `AC-UI-023` | High | `ADD-UC-013` | 手動更新と自動更新 | 更新Button、自動更新Switch、間隔[s]、最終更新時刻がある | E2E | PASS（RQU-UI-11） |
| `AC-UI-024` | High | `ADD-UC-014〜017` | データ品質・欠損・重複・時刻逆行・provenance | 不合格時はSignal/新規注文を止め、理由を表示 | E2E,UNIT | PASS（固定ダミー境界） |
| `AC-UI-025` | High | `ADD-UC-057〜066` | Signal・仮想/模擬/実注文・約定・Position | 区分が文字・アイコンで分かり、実注文は送信しない | E2E,A11Y | PASS（RQU-UI-11） |
| `AC-UI-026` | High | `ADD-UC-071/072` | 接続断・遅延・通知・再試行 | 10/30/60秒方針、停止、対応済み記録が表示される | E2E,TRACE | PASS（固定ダミー境界） |
| `AC-UI-027` | High | 全危険Dialog | Dialogに対象、影響、取消、次操作、記録項目がある | 操作結果のないButton、理由のないdisabledが0 | E2E,A11Y | PASS（RQU-UI-13） |
| `AC-UI-028` | High | 10状態 | 色だけに依存せず文字・アイコン・説明がある | 色を識別できなくても状態と次操作が読める | A11Y,MANUAL | PASS（RQU-UI-11/13） |
| `AC-UI-029` | High | 10NAV | PC・スマホから全NAVへ到達 | 10/10分類が迷わず到達可能 | E2E,VISUAL | PASS（RQU-UI-11） |
| `AC-UI-030` | High | 21画面 | スマホで横溢れ・到達不能操作がない | 主要操作が縦積み・Drawer/折畳みで到達可能 | VISUAL,E2E | PASS（遷移完了待ち後に再実行） |
| `AC-UI-031` | High | 全画面 | label、focus、keyboard、Dialog、表、コントラスト | axe Critical/Serious 0、キーボード操作可能 | A11Y,UNIT | PASS（RQU-UI-11） |
| `AC-UI-032` | Medium | `ADD-UC-080〜086` | Run/設定版/操作/承認/削除を検索・出力 | 内部記録と通常一覧の表示を分離 | E2E,TRACE | NOT-RUN |
| `AC-UI-033` | Medium | `ADD-UC-002/003/004` | Secret・Provider・Brokerの安全表示 | Secret値を表示せず、外部接続状態だけを匿名表示 | E2E,MANUAL | NOT-RUN |
| `AC-UI-034` | Medium | `ADD-UC-021/023` | 入力例と専門用語説明 | Help/用語画面から該当画面へ戻れる | E2E,DOC | NOT-RUN |
| `AC-UI-035` | Medium | Q-243安全境界 | 今回のUI確認と後続実証を区別 | 実値・外部接続・実機・LiveをPass扱いしない | TRACE,DOC,GATE | BLOCKED（後続Gate） |
| `AC-UI-036` | Medium | Q-243初期候補 | 論理候補と実シンボルを区別 | 5候補を表示し、実契約確認まで開始不可 | TRACE,DOC,GATE | BLOCKED（後続Gate） |
| `AC-UI-037` | Medium | Q-243実行可能性 | UIモックの固定ダミー・外部通信0 | モック操作は状態だけ変え、Python/Brokerへ接続しない | E2E,MANUAL | NOT-RUN |
| `AC-UI-038` | Medium | Q-243性能 | 初回3秒、追加2秒、CSV非同期、負荷時制御を確認対象化 | 実PC負荷の実測前にPassへしない | VISUAL,E2E,GATE | BLOCKED（実測待ち） |
| `AC-UI-039` | Low | 文書メタ情報 | UIモック・受入表に版、日付、入力、対象外、Unknown、変更履歴 | 単体で読める | DOC | PASS（RQU-UI-05文書） |
| `AC-UI-040` | Low | 固定ダミー | Seed、基準日時、版が固定 | 同じ条件で同じ表示・操作結果 | UNIT,E2E | NOT-RUN |

## 3. 受入確認の実行順

1. `AC-UI-001`〜`004`で追跡と画面・状態の構造を確認する。
2. `AC-UI-005`〜`021`で安全性と中核操作を確認する。Critical失敗時は以降を実行せず停止する。
3. `AC-UI-022`〜`034`で監視、レスポンシブ、アクセシビリティ、記録を確認する。
4. `AC-UI-035`〜`038`はQ-243の後続Gate境界を確認する。実値を推測してPassにしない。
5. `AC-UI-039`〜`040`と、各証拠ファイルの再現性を確認する。

## 4. 完了条件

- Critical/Highの未実行・失敗・不安定が0件。
- 全67UC、21画面、10状態、10NAVが追跡可能。
- PC/スマホ、E2E、Visual、a11yの証拠が固定条件で再実行可能。
- 外部通信、Secret、実Broker、実市場データ、実注文が0件。
- Q-243後続Gate、`RQU-UI-04-F-001〜005`、`RQU-UI-05-F-002/006`をUnknownのまま保持する。

## 5. 変更履歴

| 版 | 日付 | 内容 |
|---|---|---|
| `v0.1` | 2026-08-11 | RQU-UI-05で、Critical/High停止ルール、全UC・画面・状態・安全操作・Q-243を受入項目へ分解した。実装・E2E前の初版であり、NOT-RUNとBLOCKEDをPassにしていない。 |

## 6. RQU-UI-10統合確認項目

| 確認ID | 対象 | 確認内容 | 方法 | 状態 |
|---|---|---|---|---|
| `AC-UI-041` | `SCREEN-01`〜`SCREEN-21` | 静的HTMLに21画面の`screen-SCREEN-xx`が各1件存在する | HTML静的監査 | PASS（RQU-UI-10/11） |
| `AC-UI-042` | `nav-SCREEN-01`〜`nav-SCREEN-21` | 21導線が対応する画面へ到達し、画面内導線も機能する | Playwright/静的監査 | PASS（RQU-UI-11） |
| `AC-UI-043` | 相互リンク | UIモックから要件candidate、追跡表、受入確認表へ相対リンクで戻れる | リンク監査 | PASS（RQU-UI-10） |
| `AC-UI-044` | 外部境界 | CDN、外部フォント、外部API、Broker、実データ、Secret、実注文への参照が0件である | 文字列・ネットワーク監査 | PASS（RQU-UI-11） |
| `AC-UI-045` | ID/Trace | 重複ID、未実装画面、UC/REQの空欄を統合候補の範囲で検出し、UnknownをPassにしない | 静的監査 | PASS（RQU-UI-10/11） |

## 7. RQU-UI-11 Playwright・Visual・a11y確認項目

| 確認ID | 重要度 | 対象 | 期待値 | 証拠 | 状態 |
|---|---|---|---|---|---|
| `AC-UI-046` | Critical | Critical主要操作経路 | Backtest条件、網羅検証、結果、Forward/Shadow、Paper/Live、Human Gate、停止・復旧、警告対応をPC/スマートフォンで完了できる | `results.json` / E2E | PASS |
| `AC-UI-047` | Critical | 10共通状態 | 10状態すべてに影響と次操作があり、各状態操作がPC/スマートフォンで完了する | `results.json` / E2E | PASS |
| `AC-UI-048` | High | キーボード・Dialog | Tab移動、Dialog表示、Escape、focus復帰が確認できる | Playwright画像・結果 | PASS |
| `AC-UI-049` | High | React 21画面 | `SCREEN-01`〜`SCREEN-21`の到達性がPC/スマートフォンで21/21 | route summary | PASS |
| `AC-UI-050` | Critical | React/静的HTML a11y | axeのcritical/serious違反が4条件（React/静的HTML×PC/スマートフォン）で0 | 4件のa11y JSON | PASS |
| `AC-UI-051` | Critical | 静的HTML外部境界 | 21画面をPC/スマートフォンで表示し、外部リクエストが0 | static route summary / browser events | PASS |
| `AC-UI-052` | High | Visual baseline | 固定Seed `20260811`でPC/スマートフォンのbaseline画像を生成し、遷移完了後の再実行で差分なし | `tests/evidence/.../screenshots/`（Git管理外） | PASS（RQU-UI-13：遷移待ち、React重要画面、静的21画面＋重要7画面） |
| `AC-UI-053` | Critical | 実接続・正式化境界 | 実Broker、実市場データ、Secret、実注文を未接続のまま保持し、H3前のcandidateを正式版にしない | 統合台帳・RQU-UI-11記録 | OPEN（Unknown/Gate待ち） |

`AC-UI-053`は、今回外部接続が0件だったことを「接続可能」または「運用可能」の証明とは扱わない。正式化と実接続可否はRQU-UI-12/13およびRQU-UI-H3以降で別途判定する。

## 8. RQU-UI-12レビュー反映

RQU-UI-12で、Live自動承認のモード接続、取消・停止・再開Dialog、網羅検証入力、銘柄×時間足重複判定、Strategy版変更、PC/スマートフォンアクセス境界、Visual撮影タイミング、21画面×10状態、時間足フィルタ、静的HTMLとの意味差、性能、Backupを指摘した。High 8件はRQU-UI-13で必ず修正・再試験し、実接続・性能・Backup・アクセス境界のUnknownは後続Gateに残す。

## 9. RQU-UI-13修正・再試験結果

| Finding | 対応 | 証拠 | 状態 |
|---|---|---|---|
| F-001 Live自動承認とHuman Gate | 自動承認モードをHuman Gateへ接続し、両モードでRisk入力・確認Dialog・操作記録を通す | `RQU-UI-13 safety controls`、`results.json` | PASS |
| F-002 取消・停止・再開 | Run取消・再実行、運用単位一時停止・再開、Forward停止を確認Dialogへ統一 | 同上、component/Vitest | PASS（固定ダミー） |
| F-003 網羅検証入力 | 下限・上限・ステップを編集可能にし、設定JSON候補選択と組合せ数表示を追加 | 同上 | PASS（固定ダミー） |
| F-004 重複判定 | 売買ルールに関係なく同じ銘柄×時間足を開始不可に変更 | 同上 | PASS |
| F-005 Strategy変更 | 理由入力がない場合は保存・ロールバック・有効化変更を無効化し、確認Dialogと記録を追加 | 同上 | PASS（固定ダミー） |
| F-006 端末境界 | 認証不要を閉じた自PC／本人スマートフォンのUI方針と明記し、外部公開しない境界を候補文書へ同期 | candidate-0.6、静的HTML candidate-0.2 | PASS（実配備は後続Gate） |
| F-007 Visual撮影競合 | PC/スマホのDrawer閉鎖とCSS遷移完了を待機し、静的重要7画面も個別Visual比較 | `static-SCREEN-01/08/09/13/14/18/19` snapshots、route test | PASS |
| F-008 21画面×10状態 | React版で210セルをPC/スマホ各々操作・表示し、静的版は表示専用範囲へ限定 | `state-matrix-react-chromium-desktop/mobile.json` | PASS（React 210/210×2） |
| F-009 時間足フィルタ | SCREEN-11へH1/M30を追加 | safety controls E2E | PASS |
| F-010 静的危険操作の意味差 | 静的HTMLは表示・導線専用、Dialog/状態変更はReact版で確認することを候補文書へ明記 | candidate-0.6、static candidate-0.2 | PASS（範囲明確化） |
| F-011 性能 | 実PC負荷、20〜40単位、遅延は測定せずUnknown維持 | Q-243後続Gate | OPEN（Unknown） |
| F-012 監査・Backup | 実保存先、削除・復元、世代管理は測定せずUnknown維持 | Q-243後続Gate | OPEN（Unknown） |
| F-013 lint/Storybook警告 | 既存Fast Refresh警告は残す。機能Gateを阻害しない低優先度として記録 | `npm run lint` | OPEN（Low） |

RQU-UI-13の機械Gateは、`npm run build`、`npm run lint`、Vitest 9件、Playwright 18件（PC/スマホ各9件、E2E・Visual・a11y・外部通信・安全操作）を通過した。Playwright撮影画像とVisual baselineはGit管理対象外であり、JSON・Markdownの試験証跡だけを管理する。実Broker・実データ・Secret・性能・Backup・実配備のUnknownはPassへ変更していない。

## 10. 変更履歴（受入確認表）

| 版 | 日付 | 内容 |
|---|---|---|
| `v0.2` | 2026-08-11 | RQU-UI-11のPlaywright/Visual/a11y結果、RQU-UI-12レビュー指摘、AC-UI-052のOpen状態を追記。 |
| `v0.3` | 2026-08-11 | RQU-UI-13でHigh指摘を修正・再試験。21画面×10状態、静的重要画面Visual、危険操作・Live自動承認・重複判定・網羅検証入力を反映し、実接続・性能・Backup Unknownを維持。 |
